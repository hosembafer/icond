{
  "name": "icond-example-basic",
  "version": "1.0.0",
  "private": true,
  "description": "Basic example of using icond to generate an icon library",
  "type": "module",
  "scripts": {
    "init": "icond init",
    "fetch": "icond fetch",
    "build": "icond build",
    "publish": "icond library:publish --dry-run"
  },
  "devDependencies": {
    "icond": "^1.0.0"
  }
}
